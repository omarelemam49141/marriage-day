# Marriage day: Saturday 21 March 2026. Adjust time to your timezone if needed.
# Example: 2026-03-21T20:00:00
NEXT_PUBLIC_MARRIAGE_DATE=2026-03-21T20:00:00

# EmailJS (free tier): https://www.emailjs.com/
# TEMPLATE_ID = mosque quiz only. For "come to home" RSVP you need BOTH:
#   – Either deploy with a server + RESEND_API_KEY (recommended), then /api/rsvp sends the correct "I will come home" email, OR
#   – On static host (e.g. GitHub Pages): create a SEPARATE EmailJS template with only "تلبية دعوة البيت" + {{name}} (no quiz text) and set RSVP_TEMPLATE_ID below.
NEXT_PUBLIC_EMAILJS_SERVICE_ID=your_service_id
NEXT_PUBLIC_EMAILJS_TEMPLATE_ID=your_quiz_template_id
# Required for "come to home" on static host. Create a template that only says "I will come to the house" + {{name}}.
NEXT_PUBLIC_EMAILJS_RSVP_TEMPLATE_ID=your_rsvp_template_id
NEXT_PUBLIC_EMAILJS_PUBLIC_KEY=your_public_key

# Server-side email (RSVP + mosque quiz) via Resend. Sign up at https://resend.com
# Create an API key, verify your domain (or use onboarding domain for testing), then set:
RESEND_API_KEY=re_xxxxxxxxxxxx
# Optional: override recipient and sender (defaults used if not set)
# RSVP_TO_EMAIL=omarelemam49141@gmail.com
# RSVP_FROM_EMAIL=Ishhar <onboarding@resend.dev>
